Read /tmp/r74m_cx_shared.md. AXIS CX1 -- the kNN / NEIGHBORHOOD-PRESERVATION ("np") metric. Findings ->
/tmp/r74m_CX1.md. Read its EXACT computation (equivalence_metrics.py + how analysis.py feeds it). Decide:
is np BASIN-SENSITIVE (compares dagua's spatial neighborhoods to the REFERENCE's = positional, wrong as a
quality gate) or BASIN-INVARIANT (each layout's preservation of the GRAPH's neighborhoods, then compares
scores)? Cite lines. Is np actually the binding constraint across the 574, or is stress/cross binding?
Count per-metric failures. Is the np gate failure (where it binds) a real deficit or a margin artifact
(0.02 absolute margin vs seed variance)? Is dagua worse or better on np? Propose the corrected gate.
