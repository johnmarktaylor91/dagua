Read /tmp/r74m_cx_shared.md. AXIS CX4 -- anti-laundering CONTROLS + a PRINCIPLED quality-equivalence test.
Findings -> /tmp/r74m_CX4.md. How are chance + negative controls constructed (read the code)? r73 found a
crude "D<=R parity" rule passes 22/40 chance controls. WHY -- because quality metrics are non-discriminative
when the reference is STOCHASTIC (a cloud with no canonical layout, so distance-to-shuffled = distance-to-
correct), or because controls are mis-built? Distinguish. Then DESIGN a principled quality-equivalence test
that (a) certifies genuine FP-floor as quality-equal, (b) passes 0/40 controls, (c) EXCLUDES genuine gaps
(sugiyama 22344 vs 2805 crossings). Consider: a discriminability pre-screen (only certify where the
reference IS canonical / controls CAN separate, e.g. mean_W_R below a threshold); a two-sample POPULATION
equivalence (dagua's 100-seed quality dist vs reference's 100-seed dist) for stochastic refs instead of
per-seed paired. State the test + how to validate it against the 40 controls + how many combos it certifies.
