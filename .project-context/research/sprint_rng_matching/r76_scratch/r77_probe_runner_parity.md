<task>
r77-B2: OGDF RUNNER BINARY-vs-SOURCE PARITY CHECK (research/probe; highest priority -- an
oracle-integrity question). The r77 MAAR trace (READ:
.project-context/research/sprint_rng_matching/r75_findings/r76_IMPL_fmmm_disc_NOTES.md
"Attempt 3") found that a locally-built runner from current sources produced outputs FAR
(RMSD 0.87-0.98) from the stored benchmark_100seed_r76_refs tensors on random_dag_50 --
tensors that were generated by the COMMITTED scripts/ogdf_runner binary. Determine which of
these is true:
(A) the trace build used wrong flags/OGDF state and the committed binary DOES match a
    faithful source build (then the refs are fine and the attempt-3 trace comparisons were
    invalid -- the trace must be redone with the correct recipe), or
(B) the committed binary and committed source genuinely produce different outputs (ORACLE
    BUG: binary/source drift -- then every ogdf reference's provenance needs the answer to
    "which is canonical?").

Repo: /home/jtaylor/projects/dagua (develop; do NOT modify scripts/ogdf_runner or commit
anything -- pure probe, output file only). OGDF source: /home/jtaylor/tools/ogdf-src
(foxglove-202510; read-only -- copy to /tmp if a rebuild needs patching).

METHOD:
1. Find the EXACT build recipe for the committed binary: r75 rebuild commit `git show
   0817427` (message/files), scripts/README/Makefile/CMake hints, and the binary's own
   traces (`strings scripts/ogdf_runner | grep -i -E "gcc|clang|ogdf|version"`; `file`;
   objdump comments). Also check whether ~/tools/ogdf-src's BUILT LIBRARY (if a build dir
   exists there) has been modified since 2026-07-02 (mtimes) -- library drift counts too.
2. Rebuild in /tmp/runner-parity strictly per that recipe from the COMMITTED
   scripts/ogdf_runner.cpp + the ogdf-src library as-is.
3. PARITY TEST: run committed-binary vs fresh-build on 6 probes (random_dag_50 x
   fmmm_steps10 + gem_iters2000, deep_chain_20 x fmmm_steps100, grid_5x5 x gem_iters100,
   each seeds 100+101) with IDENTICAL payloads (generate the payload once via the benchmark
   adapter's serialization; document how). Compare outputs byte-wise and by RMSD.
4. VERDICT (A) or (B) with evidence. If (B): bisect WHAT drifted (runner cpp vs OGDF lib --
   try building runner cpp @ 0817427 against the lib, and current cpp against it) and state
   which artifact generated benchmark_100seed_r76_refs/refs2/refs3 (all made 2026-07-03/04
   by the committed binary -- so the refs match the BINARY; the question is what the binary
   corresponds to in source history).

OUTPUT: .project-context/research/sprint_rng_matching/r75_findings/r76_RUNNER_PARITY.md --
recipe found, build log summary, parity table, verdict + implications for the ogdf
reference corpus, recommendation (re-trace with correct recipe | rebuild+regen refs |
record-runner-hash tripwire addition). ASCII. Budget ~45-60 min.
</task>
<default_follow_through_policy>
Most reasonable low-risk interpretation; if the exact recipe cannot be recovered, document
the closest reproduction and label the verdict's confidence accordingly.
</default_follow_through_policy>
