## 1. Verification Summary
I read the v2 plan, both prior adversarial reviews, the live fidelity loader/statistics code, the metric implementations, and the live benchmark manifest/integrity code. The core cross-check set was the plan’s fix claims around `graph_rel_best`, `validate_sync`, coverage denominators, family mapping, Phase 2 dispatch, FIX-S seeding, QR-IO extraction, cache invalidation, and runtime scope. Spot-check count: 8 prior-review items and all 10 v2 validation questions. Citations: [plan:1](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1), [codex-v1:1](/tmp/codex_adversarial_review_output.md#L1), [claude-v1:1](/tmp/claude_adversarial_review.md#L1), [fidelity:755](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L755), [metrics:341](/home/jtaylor/projects/dagua/dagua/metrics.py#L341), [metrics:476](/home/jtaylor/projects/dagua/dagua/metrics.py#L476), [manifest:1](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L1), [integrity:20](/home/jtaylor/projects/dagua/scripts/validate_benchmark_integrity.py#L20).

## 2. Did v2 Fix v1?
- `Codex CRIT #1` higher-better normalization: `PARTIAL`. v2 replaced the broken `best/value` rule with a gap-to-best transform, but the lower-better branch still explodes near zero and the Pareto section now contradicts its own axis semantics. [codex-v1:5](/tmp/codex_adversarial_review_output.md#L5), [plan:584](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L584), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603), [plan:645](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L645)
- `Codex CRIT #2` `validate_sync()` demoted to telemetry: `PARTIAL`. The QR pipeline does this correctly, but the fidelity worklist never explicitly removes the current hard gate even though the critical-summary text says it should. [codex-v1:10](/tmp/codex_adversarial_review_output.md#L10), [plan:111](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L111), [plan:490](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L490), [plan:771](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L771)
- `Codex HIGH #9` coverage denominator fix: `PARTIAL`. v2 adds `graphs_in_family_total` and `graphs_in_family_available`, but it still uses a family-wide denominator rather than an engine-eligibility denominator. [codex-v1:46](/tmp/codex_adversarial_review_output.md#L46), [plan:624](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L624), [plan:633](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L633)
- `Codex HIGH #5` family mapping expansion: `PASS`. The missing workload tags were added. [codex-v1:26](/tmp/codex_adversarial_review_output.md#L26), [plan:538](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L538), [plan:557](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L557)
- `Codex HIGH #6` Phase 2 split into dependency chains: `PARTIAL`. The prose now says “2 dependency chains,” but the actual task DAG still has hidden cross-chain and intra-wave dependencies. [codex-v1:31](/tmp/codex_adversarial_review_output.md#L31), [plan:761](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L761), [plan:794](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L794), [plan:800](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L800)
- `Codex M14` `fidelity_add_metrics.py` drift: `PASS`. v2 now names it explicitly in B3. [codex-v1:72](/tmp/codex_adversarial_review_output.md#L72), [plan:271](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L271)
- `Codex M15` premature compare-script deletion: `PASS`. v2 explicitly defers deletion. [codex-v1:77](/tmp/codex_adversarial_review_output.md#L77), [plan:406](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L406)
- `Claude C4` delete the backwards one-sided verdict heuristic: `PASS`. A5 now says `DELETE` and shows the branch to remove. [claude-v1:23](/tmp/claude_adversarial_review.md#L23), [plan:199](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L199), [plan:203](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L203)

## 3. NEW Issues Introduced by v2
1. `HIGH` | Part 2 metric surface. The QR plan now names `aspect_ratio_deviation`, but the codebase has no such metric; `dagua.metrics` only returns raw `aspect_ratio`. Codex’s QR design treated deviation as a derived transform, not a native metric. This will break the promised shared `compute_quality_metrics()` unless the helper explicitly derives it and, because fidelity B2 still uses raw `aspect_ratio`, the shared helper now has two incompatible consumers. Fix: define the transform in the plan and decide whether the shared helper returns raw only, derived only, or both. Quotes: “`aspect_ratio_deviation`” and “derived from `aspect_ratio()`.” [plan:467](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L467), [plan:519](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L519), [plan:699](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L699), [metrics:415](/home/jtaylor/projects/dagua/dagua/metrics.py#L415), [codex-qr:210](/tmp/codex_quality_runtime_design_output.md#L210)
2. `HIGH` | QR-IO API / multiprocessing. The proposed loader API takes a live `h5_handle`, but the same section requires a multiprocessing pool. The plan never explains how workers get process-local HDF5 handles, and the current fidelity code is serial specifically because it relies on a live `_h5_file` handle “with no thread contention.” Fix: make the shared loader accept `h5_path` or use a worker initializer that opens one handle per process. [plan:460](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L460), [plan:508](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L508), [fidelity:2517](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2517), [fidelity:2560](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2560)
3. `HIGH` | QR-IO API / hidden behavior change. The proposed `load_position_tensor(...)-> tensor | None` is too weak for fidelity. Current `load_layout()` returns a rejection reason, validates shape/NaN/Inf, computes metrics unless `_skip_metrics` is set, and even has an early `positions_file is None` rejection before consulting HDF5. Refactoring fidelity to a raw tensor helper is fine; refactoring fidelity to this API is not “no behavior change.” It would also erase the rejection detail that Group E wants to preserve. Fix: split the helper into raw load and fidelity-specific validate/annotate wrappers, and have the raw loader return `(tensor, reason)` rather than bare `None`. [plan:460](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L460), [plan:470](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L470), [plan:343](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L343), [fidelity:762](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L762), [fidelity:785](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L785), [fidelity:807](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L807), [fidelity:810](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L810)
4. `HIGH` | Phase 2 DAG still inconsistent. v2 says FID-C is “Independent of FID-A/B/S,” but C1’s Tier 3 comparator is defined as “ALL `QUALITY_METRICS`,” and B2 changes `QUALITY_METRICS`. v2 also says QR-IO is “a precondition for … FID-B,” yet the “practical dispatch” omits that dependency. Cleanup2 depends on A and B by its own text, and its failures section also depends on Group E’s `rejection_breakdown`. Fix: redraw the task graph as `FID-S -> FID-B -> FID-C`, `FID-A -> FID-CLEANUP`, `FID-E -> FID-CLEANUP`, and `QR-IO -> FID-B, QR-CORE`. [plan:246](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L246), [plan:300](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L300), [plan:352](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L352), [plan:381](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L381), [plan:781](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L781), [plan:790](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L790), [plan:800](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L800)
5. `HIGH` | Cache-key strategy is underspecified and incomplete. The cache filename only keys on graph/engine/seed/metric, while the plan’s invalidation story only mentions “the metric function’s source hash.” That misses configuration changes like `stress_sources = 32`, `stress_targets = 128`, and `crossing_samples = 50_000`, which are caller-side knobs rather than function-source changes. Fix: include a serialized metric-config version in the key: profile name, sample budgets, seed policy version, and ideally a module hash rather than a function-only hash. [plan:510](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L510), [plan:511](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L511), [plan:523](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L523), [plan:526](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L526), [plan:851](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L851)
6. `MEDIUM` | B2 implementation scope is misstated. v2 says “`sampled_stress`, `crossing_rate`, `overlap_count` are NOT in `quick()`,” but `quick()` already includes `overlap_count`; only the sampling-controlled metrics are missing. Fix: correct the scope note so implementers do not touch more code than needed. [plan:265](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L265), [metrics:1219](/home/jtaylor/projects/dagua/dagua/metrics.py#L1219)

## 4. REMAINING v1 Issues That v2 Missed
1. `HIGH` | `validate_sync()` is still only half-fixed. The critical-summary text correctly demotes it to telemetry, but the explicit fidelity task list never schedules removing the current hard gate. That means the plan can still claim the v1 issue is fixed while leaving the fidelity implementation path unchanged. [plan:111](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L111), [plan:490](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L490), [plan:771](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L771)
2. `HIGH` | Coverage bias survives. v2 fixes benchmark-progress oscillation, but not variant-filtering bias. Engines capped at `max_nodes = 5_000` or `20_000` will still look under-covered on families where other engines can run larger graphs. The right denominator is engine-eligible scheduled graphs, not family-available graphs. [plan:631](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L631), [plan:633](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L633), [manifest:3780](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L3780), [manifest:5548](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5548)
3. `HIGH` | Per-metric thresholds are still arbitrary, just no longer globally arbitrary. v2 moved from one threshold scheme to per-metric constants, but it still does not ground most of them in either code-level target bands or empirical benchmark distributions. [plan:680](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L680), [metrics:210](/home/jtaylor/projects/dagua/dagua/metrics.py#L210), [metrics:249](/home/jtaylor/projects/dagua/dagua/metrics.py#L249), [metrics:329](/home/jtaylor/projects/dagua/dagua/metrics.py#L329), [metrics:450](/home/jtaylor/projects/dagua/dagua/metrics.py#L450)
4. `HIGH` | QR runtime estimate is still optimistic. The plan still reasons from “~235 engine variants” even though the live manifest is `engines = "all"`, `variants = true`, `seeds = 60`, and `total_scope = 1,267,245`; it also understates current completion by claiming “77% complete” while the manifest says `810,210 / 1,267,245`, about `63.9%`. [plan:533](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L533), [plan:819](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L819), [manifest:7](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L7), [manifest:10](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L10), [manifest:15](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L15), [manifest:5660](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5660), [manifest:5661](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5661)

## 5. Validations of Specific v2 Claims
1. `QUALIFIED` | v2 did fix several v1 criticisms on paper: family mapping, B3 drift, compare-script deletion, and explicit A5 branch deletion are real improvements; `graph_rel_best`, `validate_sync`, coverage, and Phase 2 are only partially fixed because the new text solves the first-order complaint but leaves a second-order bug behind. [plan:538](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L538), [plan:271](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L271), [plan:406](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L406), [plan:203](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L203), [plan:584](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L584), [plan:490](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L490), [plan:624](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L624), [plan:761](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L761)
2. `FAIL` | FIX-S seeding is not correct as written. `hash((graph_name, engine_name, layout_seed))` is not process-stable here; I could not cite a repo file for that because it is Python runtime behavior, but two fresh local Python processes in this environment produced different values. The good news is that the actual hook is technically viable for the targeted functions: `count_overlaps_detailed()` randomness is `torch.randperm`, `sampled_crossing_rate()` randomness is `torch.randint`, and `count_crossings()` only needs to forward the seed into its sampled branch. `sampled_stress()` is already deterministic and does not belong in FIX-S. There are no other stochastic-at-eval metrics inside `quick()` besides overlap counting, but there are other stochastic metrics elsewhere in `metrics.py` (`neighborhood_preservation`, `angular_resolution`, `cluster_separation`) if the QR surface later expands. Use the existing SHA-256-based `stable_seed()` pattern instead of Python’s builtin `hash()`. [plan:435](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L435), [plan:529](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L529), [metrics:341](/home/jtaylor/projects/dagua/dagua/metrics.py#L341), [metrics:397](/home/jtaylor/projects/dagua/dagua/metrics.py#L397), [metrics:476](/home/jtaylor/projects/dagua/dagua/metrics.py#L476), [metrics:510](/home/jtaylor/projects/dagua/dagua/metrics.py#L510), [metrics:563](/home/jtaylor/projects/dagua/dagua/metrics.py#L563), [metrics:582](/home/jtaylor/projects/dagua/dagua/metrics.py#L582), [metrics:1202](/home/jtaylor/projects/dagua/dagua/metrics.py#L1202), [metrics:1219](/home/jtaylor/projects/dagua/dagua/metrics.py#L1219), [metrics:1554](/home/jtaylor/projects/dagua/dagua/metrics.py#L1554), [metrics:618](/home/jtaylor/projects/dagua/dagua/metrics.py#L618), [metrics:670](/home/jtaylor/projects/dagua/dagua/metrics.py#L670), [metrics:744](/home/jtaylor/projects/dagua/dagua/metrics.py#L744), [fidelity:463](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L463)
3. `QUALIFIED` | QR-IO is only clean if it is narrowed to internal evaluation helpers, not a full fidelity refactor surface. Extracting a raw HDF5/PT tensor loader is reasonable, but the cited donor span is sloppy: the raw load logic is really the middle of `load_layout()`, while the surrounding function carries fidelity-only concerns like `validate_positions`, `_skip_metrics`, and `QUALITY_METRICS`. The “no behavior change” claim is false if the refactor also changes early `positions_file` handling or rejection reasons. Location-wise, `dagua/eval/` is acceptable if the module stays internal; `dagua.eval.__all__` does not export every module today, so placing it there does not automatically make it public. [plan:458](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L458), [plan:460](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L460), [plan:470](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L470), [plan:800](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L800), [fidelity:755](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L755), [fidelity:785](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L785), [fidelity:807](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L807), [fidelity:810](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L810), [fidelity:816](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L816), [eval-init:7](/home/jtaylor/projects/dagua/dagua/eval/__init__.py#L7)
4. `FAIL` | The `graph_rel_best` formula does not fully “do the right thing.” Using the plan’s code: `depth_spearman_rho(0.95,-0.10)=1.05`, `depth_spearman_rho(0.95,0.0)=0.95`, `dag_consistency(1.0,0.0)=1.0`, `dag_consistency(0.0,0.0)=0.0`, `overlap_count(best=0,value=5)=1.0`, `overlap_count(0,0)=0.0`, `sampled_stress(best=0.001,value=0.5)=499.0`. These outputs are monotone and well-defined because the guards prevent division by zero, but they are not bounded and they become nonsense when the winner is tiny but nonzero. The Pareto section then compounds this by calling both axes minimize while simultaneously saying “(1.0, 1.0) is the ideal corner” and “(0.0, 0.0) means this engine is the best AND fastest,” even though runtime best is `1.0` and quality best is `0.0`. [plan:593](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L593), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603), [plan:618](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L618), [plan:645](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L645)
5. `FAIL` | The coverage denominator fix is still incomplete. `graphs_in_family_available` solves the “nobody finished yet” oscillation, but `graphs_covered_by_engine / graphs_in_family_available` still penalizes engines that were never eligible for some graphs. The live manifest encodes engine-specific scheduling caps such as `classic_umap_default max_nodes = 20000` and `tsne_graph__for__classic_tsnet_steps200 max_nodes = 5000`; those engines should not be judged against family members they were never scheduled to run. The right denominator is `graphs_scheduled_for_engine_in_family`, with total/available reported separately as context. [plan:626](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L626), [plan:631](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L631), [plan:633](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L633), [manifest:3779](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L3779), [manifest:3780](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L3780), [manifest:5547](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5547), [manifest:5548](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5548)
6. `QUALIFIED` | The per-metric insight thresholds are better than the old one-size-fits-all rule, but they are still mostly ungrounded. Some are directionally plausible from the metric definitions: `dag_consistency` is a fraction in `[0,1]`, so `0.05/0.10` maps to a 5-10 point swing; `depth_spearman_rho` is effectively `[-1,1]` with “good” already documented as `>0.95`; `edge_straightness_mean_deg` lives in `[0,90]` because it is an `atan2` of absolute deltas, so `3°/5°` is not absurd. But `sampled_stress` and `crossing_rate` have no code-level target bands, `overlap_count` has an ideal of exactly zero so `5/20` is too blunt, and `aspect_ratio_deviation` has no implementation at all. This is still mostly arbitrary, not empirically benchmarked. [plan:680](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L680), [plan:685](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L685), [plan:687](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L687), [plan:690](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L690), [plan:694](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L694), [metrics:210](/home/jtaylor/projects/dagua/dagua/metrics.py#L210), [metrics:249](/home/jtaylor/projects/dagua/dagua/metrics.py#L249), [metrics:329](/home/jtaylor/projects/dagua/dagua/metrics.py#L329), [metrics:450](/home/jtaylor/projects/dagua/dagua/metrics.py#L450), [metrics:456](/home/jtaylor/projects/dagua/dagua/metrics.py#L456), [metrics:568](/home/jtaylor/projects/dagua/dagua/metrics.py#L568)
7. `FAIL` | The cache-key strategy is not robust enough. The plan never defines how the “source hash” is computed, so I could not verify any concrete mechanism. If it is just `inspect.getsource()` on the target metric function, it will miss transitive helper changes such as `_deterministic_sample_indices()` in `sampled_stress()` or any change in shared geometry helpers used by crossing counts. Even worse, it definitely misses plan-level sampling-budget changes because those live outside the metric function body. Hash the metric config and the module file, not just the top-level function. [plan:511](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L511), [plan:523](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L523), [plan:526](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L526), [plan:851](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L851), [metrics:39](/home/jtaylor/projects/dagua/dagua/metrics.py#L39), [metrics:531](/home/jtaylor/projects/dagua/dagua/metrics.py#L531), [metrics:605](/home/jtaylor/projects/dagua/dagua/metrics.py#L605), [metrics:1574](/home/jtaylor/projects/dagua/dagua/metrics.py#L1574)
8. `FAIL` | The QR seed-budget estimate is optimistic. The manifest confirms `seeds = 60`, `engines = "all"`, `variants = true`, and `total_scope = 1,267,245`; current completion is `810,210`. I also locally parsed `results.json` and found about `914,732` `ok` rows, which means roughly `914,732` quick-profile executions and another `914,732` sampled-profile executions if QR runs both profiles on every successful layout; the JSON does not store those derived aggregates at explicit line numbers, so I could not give a strict file-line citation for the parsed counts. Even without them, the plan’s own sampled-profile defaults imply `50,000` crossing samples per row plus stress work per row, so “5-10 hours overnight on 8 cores” is not a safe planning number. This gets worse if positions are pickled across processes instead of loaded inside workers. [plan:503](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L503), [plan:508](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L508), [plan:523](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L523), [plan:528](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L528), [plan:533](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L533), [manifest:7](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L7), [manifest:10](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L10), [manifest:15](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L15), [manifest:5660](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5660), [manifest:5661](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5661)
9. `FAIL` | The Phase 2 dispatch sequence is still wrong. `FID-CLEANUP` cannot launch with `FID-B/C/D/E` as one parallel wave because Cleanup2 explicitly depends on A and B columns and implicitly depends on E’s `rejection_breakdown` for the failures section. `FID-C` also is not independent if it compares “ALL `QUALITY_METRICS`,” and `FID-B` is textually blocked on `QR-IO`. The actual graph is at least three waves after the first one, not two. [plan:300](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L300), [plan:352](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L352), [plan:381](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L381), [plan:781](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L781), [plan:790](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L790), [plan:794](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L794), [plan:800](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L800)
10. `FAIL` | The test list is still too thin in the exact places that matter. `test_metric_seeding.py` only promises fixed-seed reproducibility, not the required “`seed=None` stays stochastic” behavior. `test_fidelity_procrustes.py` does not specify a fixture that distinguishes the correct within-original baseline from the old pooled-within bug; without a known-good/known-bad construction, A1 can regress silently. `test_pipeline_io.py` covers “HDF5 missing, .pt fallback” but not the converse precedence cases or HDF5-read-failure-then-PT-fallback behavior. [plan:444](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L444), [plan:482](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L482), [plan:742](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L742), [plan:743](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L743), [plan:744](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L744), [plan:745](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L745), [plan:753](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L753)

## 6. Polish Suggestions
- Replace every new `hash((...))` mention with the repo’s existing SHA-256 seed pattern and cite the helper directly. [plan:529](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L529), [fidelity:463](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L463)
- Define `aspect_ratio_deviation` explicitly in the plan, or remove it and use raw `aspect_ratio` everywhere. Right now the fidelity and QR metric surfaces disagree. [plan:251](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L251), [plan:519](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L519), [codex-qr:210](/tmp/codex_quality_runtime_design_output.md#L210)
- Add one sentence that `pipeline_io` is internal evaluation infrastructure and will not be re-exported from `dagua.eval.__all__`. That resolves the placement concern cleanly. [plan:458](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L458), [eval-init:11](/home/jtaylor/projects/dagua/dagua/eval/__init__.py#L11)
- Correct the stale “currently 77% complete” claim and define `coverage_ratio_for_family`, which is currently used without a definition. [plan:639](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L639), [plan:819](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L819), [manifest:5660](/home/jtaylor/projects/dagua/eval_output/variant_bench_full/manifest.json#L5660)

## 7. Verdict
`SHIP_WITH_FIXES`

The plan is close. The big remaining blockers are not stylistic; they are implementation-shaping bugs: unstable seeding via builtin `hash()`, an over-ambitious QR-IO API that is not actually “no behavior change,” a still-wrong Phase 2 DAG, incomplete coverage math under variant filtering, and a `graph_rel_best`/Pareto formulation that still produces pathological numbers. Those should be fixed in the plan before implementation dispatch.

## 8. Executive Summary
v2 is materially better than v1: it really did absorb several major critiques, especially around family mapping, stale cleanup items, and explicitly deleting the backwards Procrustes heuristic. The remaining problems are now subtler and more dangerous because they sit at architectural seams: FIX-S names the right functions but uses the wrong seed primitive; QR-IO tries to standardize loader behavior without preserving fidelity’s rejection semantics or multiprocessing constraints; the new coverage denominator still confuses failure with ineligibility; the new `graph_rel_best` formula fixes one class of bug while introducing another near zero; and the revised dispatch plan still is not the real dependency graph. I would not abandon the approach, but I would tighten those math/API/dependency points before implementation.

Codex session ID: 019d7333-8ee1-7f00-9f88-1066f63b0f32
Resume in Codex: codex resume 019d7333-8ee1-7f00-9f88-1066f63b0f32
