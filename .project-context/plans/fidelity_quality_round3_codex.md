## 1. **Verification Summary**
I read the v3 plan, the prior round-2 critique, and the live code paths the plan now depends on: `scripts/fidelity_analysis.py`, `dagua/metrics.py`, `scripts/run_benchmark.py`, `scripts/validate_benchmark_integrity.py`, `scripts/consolidate_positions_hdf5.py`, `scripts/fidelity_recompute_verdicts.py`, and `pyproject.toml`. The main cross-checks were the new `pipeline_io` API, `stable_seed` move, HDF5 worker model, `graph_rel_best`, coverage math, deterministic-comparator assumptions, and the 4-wave dispatch plan. ([plan:185](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L185), [round2:14](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L14), [fidelity:755](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L755), [metrics:476](/home/jtaylor/projects/dagua/dagua/metrics.py#L476), [run_benchmark:162](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L162), [integrity:20](/home/jtaylor/projects/dagua/scripts/validate_benchmark_integrity.py#L20), [consolidate:26](/home/jtaylor/projects/dagua/scripts/consolidate_positions_hdf5.py#L26), [pyproject:28](/home/jtaylor/projects/dagua/pyproject.toml#L28))

## 2. **Did v3 fix v2 round-2 issues?**
1. `aspect_ratio_deviation` missing: `PASS`. v3 now defines it explicitly as a derived metric and separates QR’s derived use from fidelity’s raw `aspect_ratio`. ([round2:15](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L15), [plan:336](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L336), [plan:835](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L835))
2. QR-IO multiprocessing with HDF5: `PASS`. v3 switched from a shared live handle to a worker initializer that opens one handle per process. ([round2:16](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L16), [plan:141](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L141), [plan:724](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L724))
3. QR-IO raw loader erased rejection detail: `PARTIAL`. v3 changed the API to `(tensor, reason)`, but the proposed reason strings no longer match fidelity’s current reasons, and v3’s own helper/E1 enum disagree with each other. ([round2:17](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L17), [plan:214](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L214), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603), [fidelity:785](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L785))
4. Phase/DAG inconsistency: `PASS` on dependency logic. v3’s DAG now correctly places `QR-IO -> FID-S/FID-B/QR-CORE`, `FID-B -> FID-C`, and `FID-A/FID-B/FID-E -> FID-CLEANUP`. ([round2:18](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L18), [plan:1167](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1167), [plan:1216](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1216))
5. Cache-key underspecification: `PASS`. v3 now includes sampling-config hash, whole-module `metrics.py` hash, and FIX-S version tag. ([round2:19](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L19), [plan:794](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L794))
6. B2 scope note around `overlap_count`: `PASS`. v3 explicitly corrects that `overlap_count` is already in `quick()`. ([round2:20](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L20), [plan:494](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L494), [metrics:1219](/home/jtaylor/projects/dagua/dagua/metrics.py#L1219))
7. `validate_sync()` hard gate missing from task list: `PASS`. v3 added explicit Cleanup1 to remove the hard gate, and the gate is real in current code. ([round2:23](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L23), [plan:620](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L620), [fidelity:2473](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2473))
8. Coverage denominator under variant filtering: `PASS`, assuming QR-CORE reads full `results.json` rows. Scheduler-time `skipped` rows already exist for `max_nodes` caps, so `covered / scheduled` is implementable from results data. ([round2:24](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L24), [plan:945](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L945), [run_benchmark:2126](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L2126))
9. QR runtime estimate stale at “77% complete”: `PASS/QUALIFIED`. v3 replaced that with `~914k ok rows` and `1-3 hours`, which is much better grounded, though still an estimate. ([round2:26](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L26), [plan:781](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L781), [plan:1255](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1255))
10. Test list too thin: `PARTIAL`. v3 adds `seed=None` stochasticity tests and a pooled-baseline regression fixture, but still misses explicit HDF5-precedence and HDF5-read-failure-then-PT-fallback cases. ([round2:38](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L38), [plan:1085](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1085), [plan:1126](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1126))

## 3. **NEW issues introduced by v3**
1. `HIGH` | QR-IO path API | The helper is specified as `positions_dir: Path  # <input_dir>/positions/` and then does `pt_path = positions_dir / positions_path`, but the benchmark already stores `positions_file` as `positions/<name>.pt`. That would produce `.../positions/positions/...` if implemented literally. Fix: either rename the arg to `input_dir` and use `input_dir / positions_path`, or require `positions_path` to be basename-only. ([plan:217](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L217), [plan:248](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L248), [run_benchmark:427](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L427), [run_benchmark:1462](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L1462), [fidelity:799](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L799))
2. `HIGH` | QR-IO rejection contract | v3 documents `"h5_missing_key"` as a rejection reason, but the sample code explicitly says missing HDF5 key should “fall through to .pt” and never returns that reason. It also changes fidelity’s current reason vocabulary from `missing_positions_file` / `h5_load_failure` / `contains_nan` / `tensor_not_xy` into `no_path` / `load_failure` / `nan_positions` / `shape_mismatch`, while Cleanup/E1 expects yet another enum. Fix: define one canonical raw-loader enum plus one explicit mapping for report buckets. ([plan:225](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L225), [plan:245](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L245), [plan:269](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L269), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603), [plan:1199](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1199), [fidelity:785](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L785), [fidelity:797](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L797), [fidelity:740](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L740))
3. `HIGH` | Hidden QR-IO -> FIX-S dependency | Wave 0 makes QR-IO the root, but the proposed `compute_quick_metrics()` already calls `quick(..., seed=seed)`, and `compute_sampled_metrics()` already calls `sampled_crossing_rate(..., seed=seed)` and `count_crossings(..., seed=seed)`. Those signatures do not exist yet in current `dagua/metrics.py`. Fix: either narrow Wave 0 QR-IO to loader/seed/path helpers only, or move the seeded metric helper work into FID-S. ([plan:283](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L283), [plan:304](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L304), [plan:1197](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1197), [metrics:1166](/home/jtaylor/projects/dagua/dagua/metrics.py#L1166), [metrics:563](/home/jtaylor/projects/dagua/dagua/metrics.py#L563), [metrics:1554](/home/jtaylor/projects/dagua/dagua/metrics.py#L1554))
4. `MEDIUM` | `stable_seed` move scope | The plan says moving `stable_seed` means updating the three uses in `fidelity_analysis.py`, but `scripts/fidelity_recompute_verdicts.py` also imports and calls it. Fix: count this as a fourth consumer or leave a compatibility import in `fidelity_analysis.py`. ([fidelity:1519](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L1519), [fidelity:1770](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L1770), [fidelity:1775](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L1775), [recompute:26](/home/jtaylor/projects/dagua/scripts/fidelity_recompute_verdicts.py#L26), [recompute:42](/home/jtaylor/projects/dagua/scripts/fidelity_recompute_verdicts.py#L42), [recompute:149](/home/jtaylor/projects/dagua/scripts/fidelity_recompute_verdicts.py#L149))
5. `HIGH` | Deterministic comparator Tier 1 | v3 proposes `torch.equal(orig_pos[sort_idx], reimpl_pos[sort_idx])`, but the artifact format stores only a raw `[N,2]` tensor and no node-permutation metadata. There is no basis for constructing a post hoc canonical `sort_idx`. Fix: either compare raw row order directly and document the assumption that saved tensors are already node-index ordered, or add permutation metadata before promising a sorted comparator. ([plan:537](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L537), [run_benchmark:1453](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L1453), [run_benchmark:1461](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L1461), [run_benchmark:182](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L182), [fidelity:799](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L799))

## 4. **REMAINING v2 round-2 issues that v3 missed**
1. `HIGH` | `graph_rel_best` is improved but not really solved. The hard `min(raw, 10.0)` clamp is still arbitrary, and the `1e-3` floor can flatten materially different bad cases into the same score on near-zero graphs. That matters most for zero-ideal discrete metrics like `overlap_count`. ([round2:32](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L32), [plan:903](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L903), [plan:918](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L918), [metrics:345](/home/jtaylor/projects/dagua/dagua/metrics.py#L345))
2. `MEDIUM` | Test coverage is still not complete around loader precedence/error recovery. v3 adds more cases, but it still does not explicitly cover “HDF5 key present should beat PT when both exist” or “HDF5 read error falls back to PT.” ([round2:38](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L38), [plan:1085](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1085))
3. `MEDIUM` | Thresholds are still mostly policy constants. v3 improves this by printing per-family percentiles, but the actual `THRESHOLDS` table is still not empirically derived from benchmark distributions. ([round2:25](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_quality_round2_codex.md#L25), [plan:1008](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1008), [plan:1015](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1015))

## 5. **Validations of specific v3 claims**
1. `FIX-S / stable_seed`: `PASS/QUALIFIED`. The live helper is `def stable_seed(*parts: str) -> int`, joins strings with `"::"`, and returns the first 32 bits of a SHA-256 hex digest, so `stable_seed(graph_name, engine_name, str(layout_seed))` is process-stable by construction. I could not cite a multi-process run to a repo file, but the function is pure and deterministic. ([fidelity:463](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L463), [fidelity:476](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L476), [plan:204](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L204))
2. `QR-IO API design`: `FAIL`. Current `load_layout()` uses `record.result_key` for HDF5 lookup and `record.positions_file` for PT fallback; it does not derive the key from the path. Existing reasons are `missing_positions_file`, `h5_load_failure`, `load_failure`, `not_tensor`, `tensor_not_2d`, `tensor_not_xy`, `too_few_nodes`, `node_count_mismatch`, `contains_nan`, and `contains_inf`. v3’s proposed reasons are not a superset and would force a `rejection_breakdown` remap. ([fidelity:790](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L790), [fidelity:799](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L799), [fidelity:740](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L740), [plan:214](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L214), [plan:225](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L225), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603))
3. `Multiprocessing worker initializer with h5py`: `QUALIFIED`. Opening one `h5py.File(..., "r")` per worker is the right shape and does not share the parent’s live handle. SWMR is not used anywhere I found. I could not verify generic same-file reader/writer behavior from repo code, but the checked-in writer path does not write `positions.h5` in place anyway: benchmark writes `.pt`, and HDF5 consolidation writes a temp file then renames it. ([plan:724](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L724), [plan:260](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L260), [fidelity:2516](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2516), [run_benchmark:1454](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L1454), [consolidate:50](/home/jtaylor/projects/dagua/scripts/consolidate_positions_hdf5.py#L50), [consolidate:80](/home/jtaylor/projects/dagua/scripts/consolidate_positions_hdf5.py#L80))
4. ``graph_rel_best`` clamp at `10.0`: `QUALIFIED/FAIL`. The clamp is arbitrary; I found no benchmark-derived rationale. For `sampled_stress`, the raw metric itself is bounded tightly because positions are normalized to the unit square and the per-pair stress term is `(1 - d_euclidean / d_graph)^2`, so the clamp is mostly a denominator artifact, not a response to a truly unbounded metric. The `1e-3` floor is still lossy on near-zero families. ([plan:903](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L903), [plan:918](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L918), [metrics:523](/home/jtaylor/projects/dagua/dagua/metrics.py#L523), [metrics:550](/home/jtaylor/projects/dagua/dagua/metrics.py#L550), [metrics:556](/home/jtaylor/projects/dagua/dagua/metrics.py#L556))
5. `Pareto ideal corner`: `PASS`. The math is now consistent: runtime best is `1.0`, quality best is `0.0`, and the plan now states the ideal corner as `(1.0, 0.0)`. The “log-x when spread > 10x” rule is a presentation choice, not a correctness bug. ([plan:977](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L977), [plan:981](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L981), [plan:992](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L992))
6. `Coverage denominator with variant filtering`: `PASS`. The implementation can infer “scheduled” from `results.json` because the scheduler writes `skipped` rows for `max_nodes` caps before execution. v3’s `covered / scheduled` pseudocode matches that behavior. ([plan:945](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L945), [plan:960](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L960), [run_benchmark:2126](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L2126), [run_benchmark:2139](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L2139))
7. `Cache key strategy`: `PASS/QUALIFIED`. Hashing the whole `dagua/metrics.py` file is coarse but it does catch the currently relevant transitive helpers because `_ensure_cpu`, `_deterministic_sample_indices`, `segments_intersect`, and `count_crossings` all live in that file. It is broader than necessary, but not too narrow for today’s code. ([plan:794](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L794), [metrics:35](/home/jtaylor/projects/dagua/dagua/metrics.py#L35), [metrics:39](/home/jtaylor/projects/dagua/dagua/metrics.py#L39), [metrics:146](/home/jtaylor/projects/dagua/dagua/metrics.py#L146), [metrics:1554](/home/jtaylor/projects/dagua/dagua/metrics.py#L1554))
8. `Dispatch wave conflicts`: `FAIL`. The dependency graph is better, but Wave 0 still puts three tasks on one file, and one of them is the heavy behavior-changing refactor. That is merge-rebase pain waiting to happen even if the hunks do not literally collide. ([plan:1197](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1197), [plan:1204](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1204), [fidelity:58](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L58), [fidelity:755](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L755), [fidelity:2257](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2257))
9. `FID-C Tier 1 sort`: `FAIL`. The benchmark format does not preserve any node permutation, so “canonical node ordering” cannot be reconstructed from the saved artifact. If row order is canonical, just compare raw tensors. ([plan:537](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L537), [run_benchmark:1461](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L1461), [run_benchmark:182](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L182), [fidelity:823](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L823))
10. `Cleanup1 validate_sync hard gate removal`: `PASS`. `validate_sync()` is definitely called from the fidelity path, and it definitely can abort the run today. Cleanup1 is not dead weight. ([plan:620](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L620), [fidelity:2479](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2479), [fidelity:2499](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L2499))
11. `test_fidelity_procrustes.py` pooled-baseline fixture`: `QUALIFIED`. The bug is real because current code pools `orig-orig` and `reimpl-reimpl` RMSD into one “within” baseline. A higher-variance reimpl distribution can therefore mask a systematic shift. The fixture is achievable, but the prose needs to define success operationally against the old vs new comparator rather than with hand-wavy Gaussian constants. ([plan:1126](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1126), [fidelity:1814](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L1814), [fidelity:1816](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L1816))
12. `11 tasks in 4 waves`: `QUALIFIED`. Four waves is workable, but not minimal. The simplest compression is to fold FID-G into any other fidelity task and fold Cleanup1 into QR-IO or FID-E; the real blocker is not wave count, it is unresolved API shape in QR-IO. ([plan:1229](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1229), [plan:620](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L620), [plan:1198](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1198))

## 6. **Risks for implementation dispatch**
- QR-IO is not cleanly dispatchable as Wave 0 if it includes seeded metric helpers; those calls need FIX-S signatures first. ([plan:283](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L283), [metrics:1166](/home/jtaylor/projects/dagua/dagua/metrics.py#L1166))
- The loader API needs one final freeze on path semantics and rejection enums before coding, or FID-E/report/tests will all churn. ([plan:214](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L214), [plan:603](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L603))
- `stable_seed` move requires touching `scripts/fidelity_recompute_verdicts.py` too, not just fidelity proper. ([recompute:26](/home/jtaylor/projects/dagua/scripts/fidelity_recompute_verdicts.py#L26), [recompute:149](/home/jtaylor/projects/dagua/scripts/fidelity_recompute_verdicts.py#L149))
- Wave 0 parallelism is likely to waste time in rebases because QR-IO, FID-D, and FID-G all hit `scripts/fidelity_analysis.py`. ([plan:1233](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L1233), [fidelity:58](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L58), [fidelity:755](/home/jtaylor/projects/dagua/scripts/fidelity_analysis.py#L755))
- QR-CORE must read full results rows, not only `ok` rows, or the new coverage denominator silently regresses. ([plan:945](/home/jtaylor/projects/dagua/.project-context/plans/fidelity_and_quality_pipeline_plan.md#L945), [run_benchmark:2126](/home/jtaylor/projects/dagua/scripts/run_benchmark.py#L2126))

## 7. **Verdict**
`SHIP_WITH_FIXES`

## 8. **One-paragraph executive summary**
v3 materially improves the plan: the dependency DAG is now mostly correct, `stable_seed` is the right seed primitive, `validate_sync()` finally has an explicit cleanup task, coverage can now be computed as `covered / scheduled`, and the cache key is much stronger than v2’s. The remaining blockers are implementation-shaping, not cosmetic: the proposed `load_position_tensor()` path join is wrong for the actual benchmark artifacts, the rejection-reason contract is internally inconsistent and no longer matches fidelity’s current enum, QR-IO is still over-scoped for Wave 0 because its seeded metric helpers depend on FIX-S changes that do not exist yet, the deterministic Tier-1 sort has no supporting metadata, and the new `graph_rel_best` clamp/floor still hides severity on near-zero graphs. Tighten those points before dispatch and the plan is ready.

Codex session ID: 019d7375-bf4f-7410-a25d-83da5d3dde40
Resume in Codex: codex resume 019d7375-bf4f-7410-a25d-83da5d3dde40
